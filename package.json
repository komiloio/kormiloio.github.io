{
  "name": "kormilo-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html thank-you.html styles.css CNAME .nojekyll dist/ && cp -R assets dist/assets"
  }
}
